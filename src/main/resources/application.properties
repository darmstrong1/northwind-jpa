#JDBC
#jdbc.url=jdbc:postgresql://localhost:5444/northwind
#jdbc.driverClassName=org.postgresql.Driver

# Mem - initializes with the install script
#jdbc.url=jdbc:h2:mem;INIT=runscript from 'classpath:northwind.h2.sql'

#Embedded
#jdbc.url=jdbc:h2:~/northwind

#Server
jdbc.url=jdbc:h2:tcp://localhost/~/northwind

jdbc.driver=org.h2.Driver
jdbc.username=sa
jdbc.password=

#Hibernate Configuration
hibernate.dialect=org.hibernate.dialect.H2Dialect
#hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
hibernate.format_sql=true
#hibernate.hbm2ddl.auto=create-drop
hibernate.ejb.naming_strategy=org.hibernate.cfg.ImprovedNamingStrategy
hibernate.show_sql=true


#MessageSource
message.source.basename=i18n/messages
message.source.use.code.as.default.message=true

#EntityManager
#Declares the base package of the entity classes
entitymanager.packages.to.scan=co.da.nw.domain